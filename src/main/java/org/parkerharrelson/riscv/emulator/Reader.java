package org.parkerharrelson.riscv.emulator;

import java.io.IOException;

public class Reader {

    // method responsible for reading contents from ELF file into the Machine's RAM
    // Takes the file path provided through the command line and extracts contents
    // and places them into their assigned places in memory
    public void readElfFile(byte[] memory, String filePath) throws IOException {

    }
}
